name: Export Solution from Power Platform

on:
  workflow_dispatch:

jobs:
  export:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Power Platform Tools
        uses: microsoft/powerplatform-actions/actions-install@v1

      - name: Export Solution from Dev Environment
        uses: microsoft/powerplatform-actions/export-solution@v1
        with:
          environment-url: 'https://orgd10acd81.crm.dynamics.com'
          app-id: ${{ secrets.PP_CLIENT_ID }}
          client-secret: ${{ secrets.PP_CLIENT_SECRET }}
          tenant-id: ${{ secrets.PP_TENANT_ID }}
          solution-name: 'MyTestSolution'
          solution-output-file: ./exported/solution.zip
          managed: false

      - name: Unpack Solution into Files
        uses: microsoft/powerplatform-actions/unpack-solution@v1
        with:
          solution-file: ./exported/solution.zip
          solution-folder: ./solutions/MyTestSolution
          solution-type: Unmanaged
          overwrite-files: true

      - name: Commit Solution to GitHub
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add ./solutions/
          git commit -m "Exported solution: MyTestSolution"
          git push
```

---

## Save It
```
1. Scroll down to the bottom of the page
2. You will see:

┌─────────────────────────────────────────┐
│  Commit changes                         │
├─────────────────────────────────────────┤
│  Commit message:                        │
│  [ Clean workflow file            ]     │
│                                         │
│  ● Commit directly to main branch       │
│                                         │
│  [ Commit changes ]  ← CLICK THIS      │
└─────────────────────────────────────────┘

3. Click "Commit changes"
4. Click "Commit changes" again on popup
```

---

## Now Run the Workflow
```
1. Click "Actions" tab
2. Click your workflow on the LEFT
3. Click "Run workflow" on the RIGHT
4. Click green "Run workflow" button
```

---

## What to Expect
```
If secrets and App User are set up correctly:
✅ Checkout Repository
✅ Install Power Platform Tools
✅ Export Solution from Dev Environment
✅ Unpack Solution into Files
✅ Commit Solution to GitHub

If still failing → share the new error screenshot
and I will fix it immediately! 😊
